#include "parallel_insertion.h"
#include "logging.h"

#include <random>

namespace vrp {
    namespace detail {
        std::vector<Solution> parallel_insertion(const Problem& prob,
                                                    size_t count) {
            return {};
        }
    }  // namespace detail
}  // namespace vrp